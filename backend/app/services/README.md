# Services

- `ai_content.py`: OpenRouter content generation with a no-key fallback for local testing.
- `video_renderer.py`: Pillow + FFmpeg vertical reel renderer.
- `storage.py`: Local development URLs or Cloudinary signed video upload.
- `meta.py`: Instagram Reels and Facebook Page publishing through Meta Graph API.

